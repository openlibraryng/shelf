// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "ng.openlibrary.shelf"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
